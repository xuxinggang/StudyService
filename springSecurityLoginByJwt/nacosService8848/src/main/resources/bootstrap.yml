spring:
  application:
    name: naocsService
  profiles:
    active: sit
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8850   #注册中心地址
        config:
          file-extension: yml

